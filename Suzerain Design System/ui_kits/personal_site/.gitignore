# Local production build output (see build.sh)
dist/

# Python
__pycache__/
*.pyc

# Upstream TopoJSON for the maps — re-downloadable with
# scripts/geo/build-geo.py --fetch; the projected output in data/ is committed.
scripts/geo/cache/

# Editor/backup cruft
*.bak
